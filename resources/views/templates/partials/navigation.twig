<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {% if auth.check %}
        {% if auth.isStagiaire %}
      <a class="navbar-brand" href="{{ path_for('stagiaire.index') }}">Stagiaire</a>
    </div>

    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-left">
        <li><a href="{{ path_for('stagiaire.index') }}">Modules</a></li>
        <li><a href="{{ path_for('stagiaire.index') }}">Evaluation</a></li>
        <li><a href="{{ path_for('stagiaire.index') }}">Historique</a></li>
      </ul>
        {% endif %}
      {% endif %}

      {% if auth.check %}
        {% if auth.isFormateur%}
      <a class="navbar-brand" href="{{ path_for('formateur.index') }}">Formateur</a>
    </div>

    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-left">
        <li><a href="{{ path_for('formateur.index') }}">Stagiaire</a></li>
        <li><a href="{{ path_for('formateur.index') }}">Modules</a></li>
        <li><a href="{{ path_for('formateur.index') }}">Historique</a></li>
      </ul>
        {% endif %}
      {% endif %}
      
      {% if auth.check %}
        {% if auth.isAdmin %}
        <a class="navbar-brand" href="{{ path_for('admin.index') }}">Administrateur</a>
      </div>
  
      <div id="navbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav navbar-left">
        <li><a href="{{ path_for('admin.index') }}">Liste des utilisateurs</a></li>
        <li><a href="{{ path_for('admin.user.register') }}">Enregistrer un nouvel utilisateur</a></li>
        {% endif %}
      {% endif %}
      
      <ul class="nav navbar-nav navbar-right">
        {% if auth.check %}
        <li>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            {{ auth.user.username }}
            <span class="caret"></span>
          </a>
          
          <ul class="dropdown-menu">
            <li><a href="#"><span class="glyphicon glyphicon-cog"></span>Paramètres</a></li>
            <li><a href="{{ path_for('user.logout') }}"><span class="glyphicon glyphicon-log-out"></span>Déconnexion</a></li>
          </ul>
        </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>
